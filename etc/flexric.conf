# near-RT RIC configuration.
# NEAR_RIC_IP is rewritten by core-setup.sh to the RIC container's bridge IP so
# that gNB E2 agents on the cell nodes can reach it across the experimental LAN.
[NEAR-RIC]
NEAR_RIC_IP = 192.168.71.142
[XAPP]
DB_DIR = /tmp/
